# Mode: custom_shot_list

Use the provided shot list exactly. Preserve the reference subject, wardrobe, styling, materials, colors, hair, makeup, environment, lighting, and color grade in every shot. Each shot changes only camera placement, crop, composition, lens behavior, or focus target.

For each shot:
- Generate a separate prompt unless layout.type is contact_sheet or grid.
- Describe final camera position and subject state only.
- Include continuity locks and negative constraints.
- Use the requested aspect ratio.

Shot list:
{{shot_list}}
