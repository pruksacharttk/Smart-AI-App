{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Slide Start-Frame Video Prompt Output",
  "description": "The result is one text-only prompt. It must not be split into positive prompt and negative prompt.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "prompt"
  ],
  "properties": {
    "prompt": {
      "title": "Generated video prompt",
      "description": "A single concise integrated descriptive prompt for image-to-video generation. It includes motion instructions and constraints in one compact text block only; no separate negative prompt.",
      "type": "string",
      "minLength": 80,
      "maxLength": 1600
    }
  }
}